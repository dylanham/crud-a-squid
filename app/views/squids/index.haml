-@squids.each do |squid|
  %p= link_to "Name: #{squid.name}", squid_path(squid)
  %p= "Type: #{squid.squid_type}"
  %p= "Age: #{squid.age}"
  %p
    = link_to 'Edit', edit_squid_path(squid)
    = link_to 'Delete', squid_path(squid), method: :delete
  %hr
%p= link_to 'New Squid', new_squid_path
